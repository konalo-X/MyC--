成员函数count（）接受键作为参数，并返回具有该键的元素数目
成员函数low_bound（）和upper_bound() 将键作为参数，返回第一个不小于该键参数的成员，返回第一个大于该键的成员，配合使用则返回一个区间，例如copy（Ob.low_bound("a"),Ob.upper_bound("d"),out)
equal_range() 用键做参数，返回两个迭代器，表示该区间与键匹配，返回一个pair对象（封装 这两个迭代器）
